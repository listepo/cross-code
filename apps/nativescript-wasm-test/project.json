{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "nativescript-wasm-test",
  "projectType": "application",
  "sourceRoot": "apps/nativescript-wasm-test/app",
  "targets": {
    "typecheck": {
      "command": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.spec.json",
      "options": {
        "cwd": "{projectRoot}"
      },
      "dependsOn": ["^build"],
      "cache": true,
      "inputs": [
        "{projectRoot}/app/**/*",
        "{projectRoot}/references.d.ts",
        "{projectRoot}/tsconfig.json",
        "{projectRoot}/tsconfig.spec.json",
        "{projectRoot}/vitest.*.config.mts",
        "{projectRoot}/webpack.config.js",
        "^production"
      ]
    },
    "test.ios": {
      "command": "pnpm run test.ios",
      "options": {
        "cwd": "{projectRoot}"
      },
      "dependsOn": ["^build"],
      "cache": false
    },
    "test.android": {
      "command": "pnpm run test.android",
      "options": {
        "cwd": "{projectRoot}"
      },
      "dependsOn": ["^build"],
      "cache": false
    }
  }
}
