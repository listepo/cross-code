[package]
name = "wamr-sys"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Raw Rust FFI bindings to WAMR (WebAssembly Micro Runtime) — generated by bindgen + compiled via cc"

[features]
regenerate = []

[build-dependencies]
bindgen = "0.72"
cc = "1"
