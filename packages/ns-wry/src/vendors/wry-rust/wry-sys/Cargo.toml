[package]
name = "wry-sys"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Low-level Rust bindings and C-shim surface for the wry engine"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
