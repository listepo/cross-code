// NSCWry.h
//
// Umbrella header for the NSCWry framework. The implementation is Swift
// (@objc classes in NSCWry.swift); these headers mirror the @objc surface so
// the NativeScript iOS metadata generator can see it.
//
// NOTE: the class declarations live in NSCWryClasses.h, NOT here — the
// metadata generator only records classes declared in subheaders imported by
// the umbrella header; classes declared inline in the umbrella are skipped.
#import "NSCWryClasses.h"
