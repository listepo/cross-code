// Umbrella header for the NSWasmKit framework. The implementation is Swift
// (@objc classes in NSWasmKit.swift); these headers mirror the @objc surface so
// the NativeScript iOS metadata generator can see it.
//
// NOTE: the class declarations live in NSWasmKitClasses.h, NOT here -- the
// metadata generator only records classes declared in subheaders imported by
// the umbrella header; classes declared inline in the umbrella are skipped.
#import "NSWasmKitClasses.h"
