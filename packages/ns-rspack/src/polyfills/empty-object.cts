/**
 * Stands in for the `mdn-data/css/*.json` files css-tree tries to patch its
 * own data with. css-tree ships complete data already, and the JSON blobs are
 * hundreds of kilobytes of dead weight on device.
 */
export = {}
