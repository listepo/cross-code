export { WasmError as WasmEdgeError } from '@cross-code/ns-wasm-core';
export { WasmError } from '@cross-code/ns-wasm-core';
export { hostResultToWire, fromWireAll, parseSignature, toWire, fromWire, unwrapResults,
  type WasmArg, type WasmValue, type WasmValueType, type WireValue, type ParsedSignature,
} from '@cross-code/ns-wasm-core';
