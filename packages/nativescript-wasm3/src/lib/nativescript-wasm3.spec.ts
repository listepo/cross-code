import { nativescriptWasm3 } from './nativescript-wasm3.js';

describe('nativescriptWasm3', () => {
  it('should work', () => {
    expect(nativescriptWasm3()).toEqual('nativescript-wasm3');
  });
});
