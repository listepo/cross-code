plugins {
    id("com.android.library")
}

android {
    namespace = "uniffi.wasm3_ffi"
    compileSdk = 35

    defaultConfig {
        minSdk = 21
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
}

dependencies {
    // UniFFI-generated bindings depend on the UniFFI Kotlin runtime
    implementation("net.java.dev.jna:jna:5.15.0@aar")
}

// UniFFI Kotlin bindings are generated by build.sh and committed.
// The library just packages them as an AAR for NativeScript consumption.
