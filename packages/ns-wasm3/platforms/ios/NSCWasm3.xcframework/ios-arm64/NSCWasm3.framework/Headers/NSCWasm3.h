// NSCWasm3.h
//
// Umbrella header for the NSCWasm3 framework. The implementation is Swift
// (@objc classes in NSCWasm3.swift); these headers mirror the @objc surface so
// the NativeScript iOS metadata generator can see it.
//
// NOTE: the class declarations live in NSCWasm3Classes.h, NOT here — the
// metadata generator only records classes declared in subheaders imported by
// the umbrella header; classes declared inline in the umbrella are skipped.
#import "NSCWasm3Classes.h"
